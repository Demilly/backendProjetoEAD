{
  "start": "java -jar build/libs/*.jar",
  "install": "echo 'compiling' && ./gradlew build",
  "watch": {
    "throttle": 500,
    "install": {
      "include": [
        "^build\\.gradle$",
        "\\.java$"
      ]
    }
  }
}